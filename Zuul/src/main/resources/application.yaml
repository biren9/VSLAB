server:
  port: 8081
  
eureka:  instance:
    lease-renewal-interval-in-seconds: 10
    status-page-url-path: /info
    health-check-url-path: /health
    
zuul:
  ignored-services: '*'
  routes:
    user-proxy: /user-api/**
    #Todo: öffentliche api proxies bereitstellen 